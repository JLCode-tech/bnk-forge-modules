{{- if not (index .Values "f5-tmm").tmm.Only }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "f5ingress.configName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "f5ingress.labels" . | nindent 4 }}
data:
{{- if .Values.controller.config.entries }}
{{ toYaml .Values.controller.config.entries | indent 2 }}
{{- end }}
{{- end }}
