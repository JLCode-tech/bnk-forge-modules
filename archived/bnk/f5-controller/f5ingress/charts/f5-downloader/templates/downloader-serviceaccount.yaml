{{- if and .Values.enabled .Values.downloader.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "downloader.serviceAccountName" . }}
  namespace: {{ $.Release.Namespace }}
{{- end }}
