{{- if and .Values.enabled .Values.tmm.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tmm.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
