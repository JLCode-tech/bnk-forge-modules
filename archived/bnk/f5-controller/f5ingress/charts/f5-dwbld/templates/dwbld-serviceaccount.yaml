{{- if and .Values.enabled .Values.dwbld.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dwbld.serviceAccountName" . }}
  namespace: {{ $.Release.Namespace }}
{{- end }}